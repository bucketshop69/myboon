{
  "cli": {
    "version": ">= 14.0.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "installDependencies": "pnpm install --frozen-lockfile"
    },
    "dapp-store": {
      "distribution": "internal",
      "installDependencies": "pnpm install --frozen-lockfile",
      "android": {
        "buildType": "apk"
      }
    },
    "preview": {
      "distribution": "internal",
      "installDependencies": "pnpm install --frozen-lockfile",
      "android": {
        "buildType": "apk"
      }
    },
    "production": {
      "installDependencies": "pnpm install --frozen-lockfile",
      "android": {
        "buildType": "app-bundle"
      }
    }
  },
  "submit": {
    "production": {}
  }
}
